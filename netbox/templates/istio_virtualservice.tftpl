---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  labels:
    app: netbox
  name: netbox
  namespace: ${ namespace }
spec:
  gateways:
  - netbox
  hosts:
  - ${ host_name }
  http:
  - route:
    - destination:
        host: netbox
    timeout: 30s
